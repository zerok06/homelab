[Unit]
Description=Esperar IP de Tailscale antes de arrancar Docker
After=tailscaled.service
Before=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/homelab-wait-tailscale.sh
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
